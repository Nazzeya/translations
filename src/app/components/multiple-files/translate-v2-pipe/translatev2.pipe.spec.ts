import { Translatev2Pipe } from './translatev2.pipe';

describe('Translatev2Pipe', () => {
  it('create an instance', () => {
    const pipe = new Translatev2Pipe();
    expect(pipe).toBeTruthy();
  });
});
