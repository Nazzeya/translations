import { TranslateOnePipe } from './translate-one.pipe';

describe('TranslateOnePipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateOnePipe();
    expect(pipe).toBeTruthy();
  });
});
