import { TranslateAllPipe } from './translate-all.pipe';

describe('TranslateAllPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateAllPipe();
    expect(pipe).toBeTruthy();
  });
});
